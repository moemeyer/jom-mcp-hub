# Python dependencies for JOM MCP Servers
# Used by all agency deployments (NORIVO, EXLYAR, JOM)

# MCP SDK
fastmcp>=0.2.0
mcp>=1.27.0

# Web framework (streamable-http transport)
starlette>=0.37.0
uvicorn[standard]>=0.27.0

# HTTP client for GHL API calls
httpx>=0.27.0

# GCP Secret Manager
google-cloud-secret-manager>=2.16.0

# AWS SDK for DynamoDB (optional auth code storage)
boto3>=1.34.0

# JSON schema validation
jsonschema>=4.21.0

# Logging
python-json-logger>=2.0.7

# Environment variable management
python-dotenv>=1.0.0
